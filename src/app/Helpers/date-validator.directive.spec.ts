import { DateValidatorDirective } from './date-validator.directive';

describe('LoanDateValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new DateValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
